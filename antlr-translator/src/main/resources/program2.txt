// Comment

/* Multiline
    comment  */

var [a, b, c]: int = read("%d %d %d")

function main(): int {
    var [a, b, c]: int = read("%d %d %d")

    var arr: int* = {0, 1, 2}

    /*for (c, [i] : arr) {
        string str = "a[%d] = %d" << (i, c)
        print(str)
    }*/

    [a, b, c] = [b, c, a]

    a[3] = 3

    [a, b, c] = print("a: %d, b: %d, c: %d")
}

